# TEMPLATE ONLY — replace every placeholder before applying, and do not commit
# the filled-in version to version control. Prefer a secrets manager
# (e.g. Sealed Secrets, External Secrets Operator, Vault) for production.
#
# Quick local fill-in:
#   kubectl create secret generic ems-secrets -n ems \
#     --from-literal=DB_USERNAME=ems_user \
#     --from-literal=DB_PASSWORD=change_me \
#     --from-literal=DB_ROOT_PASSWORD=change_me_root \
#     --from-literal=JWT_SECRET=$(openssl rand -base64 64) \
#     --from-literal=DEFAULT_ADMIN_PASSWORD=change_me_admin \
#     --from-literal=UNSPLASH_ACCESS_KEY=your_unsplash_key
#
apiVersion: v1
kind: Secret
metadata:
  name: ems-secrets
  namespace: ems
type: Opaque
stringData:
  DB_USERNAME: "REPLACE_ME"
  DB_PASSWORD: "REPLACE_ME"
  DB_ROOT_PASSWORD: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME_WITH_OPENSSL_RAND_BASE64_64"
  DEFAULT_ADMIN_PASSWORD: "REPLACE_ME"
  UNSPLASH_ACCESS_KEY: "REPLACE_ME"
